# 游戏代码重构 AI 专家指导手册 (AI Refactoring Guidelines)

## 🎯 你的角色定位

你现在是一位拥有15年经验的**资深游戏引擎程序员**和**软件架构专家**。你精通游戏开发中的设计模式、性能优化（特别是大型开放世界游戏）、SOLID原则
你的任务是协助我审查和重构现有的游戏代码，消除技术债务，降低系统熵增，并确保代码达到工业级标准。

## ⚠️ 核心重构铁律 (绝不能违反)

1. **行为保持不变**：重构不是重写或添加新功能。重构后的代码必须100%保持原有的业务逻辑和外部表现。
2. **拒绝过度工程**：使用简单、清晰的设计模式。不要为了模式而模式，引入不必要的复杂性。
3. **防御性编程**：所有对外部组件、资源的获取必须有判空逻辑和错误提示（Error Logging）。
4. **显式报错**：任何可能不是预期的行为，都必须通过日志，控制台或者弹窗等等方式报告，不允许隐式错误。

---

## 🔍 重构审查清单 (Refactoring Checklist)

在阅读我提供的代码时，请严格按照以下四个维度进行分析和重构：

### 1. 架构与解耦 (Architecture & Decoupling)

- **消除硬编码与强耦合**：类与类之间不应直接互相引用。引入接口（Interfaces）、事件系统（Event/Observer Pattern）或依赖注入。
- **单一职责原则 (SRP)**：如果一个脚本既处理玩家输入，又处理动画，还处理网络同步，**必须将其拆分**。
- **隔离UI与逻辑**：游戏核心逻辑绝不能直接引用UI组件，必须通过事件或数据绑定的方式进行通信。
- **数据与表现分离**：考虑使用godot的最佳方案来存储配置数据，而不是写死在代码里。

### 2. 性能优化 (Performance - 开放世界特化)

- **消灭每帧垃圾回收 (Zero GC in Update)**：检查 `Update`/`Tick` 函数中是否有任何 `new` 操作、字符串拼接或装箱/拆箱操作。
- **组件缓存**：禁止在频繁调用的函数中使用 `GetComponent()` 或 `FindObjectOfType()`，必须在 `Awake`/`Start` 中缓存引用。
- **对象池 (Object Pooling)**：检查是否有频繁的 `Instantiate` 和 `Destroy`，如果有，将其重构为使用对象池。
- **空间与时间分片**：对于遍历大量实体的逻辑（如视野检测、AI寻路），如果不必要每帧执行，请将其重构为定时执行（协程/计时器）或使用空间划分结构。

### 3. 代码规范与可读性 (Coding Standards)

- **命名规范**：按照C#最佳命名规范
- **卫语句 (Early Return)**：消除深层嵌套的 `if-else`。遇到不符合条件的情况立刻 `return`。
- **消除魔法数字**：所有的常量数字、字符串必须提取为 `const` 变量或 `enum`，并加上注释说明用途。
- **无用代码清理**：删除所有被注释掉的代码、未使用的变量和没有实际逻辑的空方法。

### 4. 最佳实践 (Best Practices)

- 优先使用组合（Composition）而不是继承（Inheritance）。
- 状态机（FSM）或行为树：如果有复杂的嵌套状态判断（如AI或动画），重构为状态机模式。
- 合理使用只读（`readonly`）和常量。

### 特殊注意：

如果对于一个方案，可以从根本上优化，就不要在屎山的基础上优化，直接从零实现即可。

---

## 🔄 AI 重构标准工作流 (Standard Workflow)

当我提交一段代码要求重构时，你必须按照以下步骤分析：

### 第一步：代码诊断 (Diagnosis)

简要列出当前代码中存在的严重的问题（例如：耦合度高、存在GC、命名不规范等）

### 第二步：确认边缘条件和接口

确保向外暴露的所有接口不能修改。如果需要提供更新的接口，需要把旧接口标记为废弃，并保留下来以保持兼容性。

### 第三步：彻底重构

检查并重构所有需要重构的文件，以最高标准完成，确保是最佳实现。

### 第四步：彻底检查

1. 确保能够正常编译。
2. 确保没有对原有功能进行破坏，确保接口不变。
3. 确保测试完全通过
4. 对于编译过程中的警告，必须修复其中暴露的质量问题

### 第五步：输出报告

将重构的所有文件的修改摘要全部写入一个重构日志中。如果有接口变更，必须详细说明。

---

## 本轮重构日志

### 2026-05-09 存档与世界种子基础设施

本轮新增并接入了开放世界运行时所需的第一版正式持久化链路，目标是为后续世界规律、行为画像、剧情导演和地图过渡系统提供稳定底座。

#### 新增内容

- 新增 `Game58date/Save` 目录，建立 `GameSaveData` 与 `GameSaveRepository`
- 新增世界种子、玩家位姿、区块覆盖的 JSON 存档格式
- 新增区块覆盖存档映射层 `VoxelChunkOverrideSaveMapper`
- 新增运行时启动选项解析 `TerrainRuntimeStartupOptions`
- 新增自动创建存档、自动恢复存档、自动保存和退出强制保存流程

#### 接口与兼容性

- 未破坏现有地形运行时外部入口
- `TerrainGenerationSettings` 新增 `WithSeed(int seed)`，用于在不污染默认配置对象语义的前提下恢复存档种子
- `VoxelChunkOverrideStore` 新增快照、替换、计数与修订号能力，用于支持持久化与变更检测
- `VoxelTerrainWorldRuntime` 暴露只读 `Settings` 与 `OverrideStore` 访问口，未改变原有行为接口

#### 质量控制

- 读取失败的存档会自动备份后重建，避免静默失败
- 恢复时会校验玩家位姿数据有效性
- 恢复时会校验区块尺寸与高度是否匹配，避免旧存档污染新世界结构
- 已验证 `dotnet build Game58date.Windows\\Game58date.Windows.csproj` 编译通过，0 warning，0 error

### 2026-05-09 世界规律引擎与行为画像基础版

本轮继续沿 P0 主线开发，将原型中的世界规律、因果和行为趋势逻辑拆出并正式接入默认地形运行时。

#### 新增内容

- 新增 `Game58date/Gameplay` 目录
- 新增正式领域模型 `WorldLawState`、`PlayerBehaviorProfile`、`CausalityRecord`、`WorldLawRuntimeState`
- 新增正式规则引擎 `WorldLawEngine`
- 新增存档映射层 `WorldLawSaveMapper`
- 新增正式运行时控制器 `WorldLawRuntimeController`

#### 本轮能力

- 世界规律状态持续推进
- 行为画像趋势累计
- 因果记录环形保留
- 基于输入和行为的征兆触发
- 英雄之旅阶段基础推进
- 运行时 HUD 与调试热键
- 灯光颜色与强度的基础环境反馈
- 世界规律与行为画像状态自动存档、自动恢复

#### 架构说明

- 将原型脚本内耦合的世界规律逻辑拆为独立领域层与运行时表现层
- 默认地形运行时继续作为唯一正式入口，避免形成第二套主链路
- 存档主结构从 `schemaVersion=1` 升级到 `schemaVersion=2`

#### 质量控制

- 编译验证通过：`dotnet build Game58date.Windows\\Game58date.Windows.csproj`
- 0 warning，0 error
- 规则随机源改为可持久化内部状态，保证自动保存/恢复后演进连续性

### 2026-05-09 玩家意图输入系统与剧情导演基础版

本轮继续推进 P1 主线，将默认运行时中的输入处理与剧情阶段推进从单体控制器中拆出，形成正式系统模块。

#### 新增内容

- 新增正式玩家意图系统 `PlayerIntentSystem`
- 新增正式英雄之旅导演 `HeroJourneyDirector`
- 新增运行模式切换 `RuntimeMode` / `RuntimeModeResolver`
- 扩展 `WorldLawRuntimeState`，纳入意图状态与剧情导演状态
- 扩展存档结构，持久化意图历史、最后一次解析结果、剧情阶段与阶段历史

#### 本轮能力

- 玩家文本意图可被解析为正式语义主题
- 解析结果可影响目标 biome 倾向
- 剧情导演不再依赖原型脚本内联判断，而是独立基于世界规律、行为画像和意图状态推进
- 默认运行时继续走地形主链
- 旧 `HeroJourneyPrototypeScript` 仅在 `GAME58DATE_RUNTIME_MODE=prototype` 时启用

#### 架构说明

- 将“输入采集/解析”“世界规律”“剧情导演”“表现 HUD”拆分为独立职责
- 避免默认正式入口继续和旧原型脚本混用
- 让后续征兆系统、NPC 导演和地图导演可以直接消费结构化意图与剧情状态

#### 质量控制

- 修复本轮引入的可空警告，确保 `0 warning / 0 error`
- 已验证 `dotnet build Game58date.Windows\\Game58date.Windows.csproj` 通过

### 2026-05-09 正式征兆系统基础版

本轮继续推进 P1，将征兆从世界规律引擎里的附属结果提升为独立系统，便于后续继续扩展感知技能、地图导演和事件表现层。

#### 新增内容

- 新增独立征兆导演 `OmenDirector`
- 扩展运行时状态与存档结构，新增征兆激活状态、来源、评分与历史
- 将征兆来源拆分为 `Intent / EmergentWorldLaw / Causality / Narrative`
- 将运行时 HUD 与环境反馈切换为消费正式征兆状态

#### 本轮能力

- 玩家意图可主动触发征兆
- 世界规律演化可被征兆导演评分并激活
- 因果事件可推动独立征兆
- 剧情阶段推进可生成叙事型征兆
- 征兆拥有独立冷却、可视时间、最近历史和来源标签
- 征兆状态可自动存档和自动恢复

#### 架构说明

- 避免继续将征兆逻辑写死在 `WorldLawEngine`
- 让征兆系统成为后续感知模式、特殊高亮、地图过渡和事件导演的上游输入
- 保持默认地形主运行时不变，只增加正式子系统装配

#### 质量控制

- 已验证 `dotnet build Game58date.Windows\\Game58date.Windows.csproj` 通过
- `0 warning / 0 error`

### 2026-05-09 感知技能系统与征兆表现层基础版

本轮继续沿征兆主线开发，为玩家增加主动触发的感知能力，并让征兆系统具备更明确的运行时表现层反馈。

#### 新增内容

- 新增感知技能控制器 `PerceptionSkillController`
- 扩展运行时状态与存档结构，新增感知技能激活状态、持续时间、冷却与强度
- 将 `Q` 键接入为感知技能触发入口
- 扩展 HUD，展示感知技能状态、剩余时间、冷却和强度

#### 本轮能力

- 玩家可主动开启感知技能
- 感知技能强度随当前征兆评分变化
- 感知开启后会进一步强化灯光强度与氛围反馈
- 感知状态可自动存档和自动恢复

#### 架构说明

- 感知技能不直接依赖原型脚本，而是正式消费 `OmenRuntimeState`
- 表现层继续保持在正式运行时控制器内装配，避免分叉第二套主链路
- 为后续真正的视觉滤镜、高亮和局部强调提供了稳定状态接口

#### 质量控制

- 已验证 `dotnet build Game58date.Windows\\Game58date.Windows.csproj` 通过
- `0 warning / 0 error`

### 2026-05-09 正式表现层基础版

本轮从“数据已经存在”切到“玩家能直接看见”，把正式征兆和感知状态接到了实体表现层。

#### 新增内容

- 新增正式表现控制器 `OmenPresentationController`
- 在正式运行时内创建五类征兆标记实体
- 为征兆标记接入动态点光源、缩放脉冲和颜色变化
- 将感知技能强度接入表现层，强化征兆标记与环境反馈

#### 本轮能力

- 玩家现在可以在场景中看到正式的征兆标记
- 当前高权重征兆会被突出显示
- 感知技能激活后，征兆光点、亮度和主地标响应会被显著加强
- 运行时不再只有 HUD 数据，而是具备真实世界内反馈

#### 架构说明

- 表现层独立于 `WorldLawEngine` 和 `OmenDirector`
- 运行时控制器只负责装配和状态传递，不直接内联具体视觉细节
- 这为后续继续升级到滤镜、高亮轮廓、局部环境雾效等更强表现打下基础

#### 质量控制

- 已验证 `dotnet build Game58date.Windows\\Game58date.Windows.csproj` 通过
- `0 warning / 0 error`

### 2026-05-09 商业级 UI 主链基础版

本轮开始正式 UI 开发，不再让 `DebugText` 承担主界面职责，而是建立了一套统一主题驱动的代码化 UI 主链。

#### 新增内容

- 新增 UI 主题 `GameUiTheme`
- 新增 UI 运行时控制器 `GameUiRuntimeController`
- 新增 UI 组合器 `GameUiComposer`
- 默认地形运行时自动挂接正式 UI

#### 本轮能力

- 现代化、简洁、扁平的主 HUD
- 古典/史诗感的深色底板与淡金色装饰语言
- 顶部叙事与征兆摘要区
- 右上状态卡，展示画像、感知、意图等核心状态
- 底部主输入区，作为正式意图输入界面起点
- 辅助信息侧栏，承接世界信号与引导信息
- 默认正式运行时关闭 `DebugText` 主 HUD，仅保留原型模式显示

#### 架构说明

- 当前 UI 以代码生成，避免先绑定编辑器页资源导致迭代速度过慢
- 主题、运行时控制器、组合器三层拆分，后续可继续扩展到正式菜单、弹窗和任务/征兆卡片
- 正式 UI 和当前征兆/感知/剧情状态直接联动，不再依赖中间临时字符串层

#### 质量控制

- 已验证 `dotnet build Game58date.Windows\\Game58date.Windows.csproj` 通过
- `0 warning / 0 error`

### 2026-05-09 正式 UI 信息层强化

本轮继续在正式 UI 主链上推进，把静态 HUD 扩成更完整的信息界面结构。

#### 新增内容

- 输入区增加正式引导文案
- 增加最近一次结构化意图摘要
- 增加叙事逻辑面板，展示当前剧情阶段推进原因
- 增加最近信号历史面板，展示最近三个正式征兆

#### 本轮能力

- 玩家可以更清楚地理解“为什么当前进入这个叙事阶段”
- 玩家可以追踪最近的征兆来源与强度
- UI 已开始承担信息解释职责，而不是只做状态罗列

#### 质量控制

- 已验证 `dotnet build Game58date.Windows\\Game58date.Windows.csproj` 通过
- `0 warning / 0 error`

### 2026-05-09 系统菜单基础版

本轮继续扩展正式 UI 主链，为其加入第一版可交互的系统菜单容器，而不再只停留在信息展示层。

#### 新增内容

- 增加 `System Atlas` 系统面板
- 增加存档/阶段摘要、设定摘要和菜单提示文案
- 增加按钮交互入口
- 增加 `Esc / F10` 热键开关菜单

#### 本轮能力

- 正式 UI 已经具备菜单层容器
- 玩家可以通过热键打开/关闭系统面板
- 菜单内已具备意图焦点切换和调试 HUD 切换入口
- 商业级 UI 方案从“HUD”拓展到“HUB + 菜单”双层结构

#### 质量控制

- 已验证 `dotnet build Game58date.Windows\\Game58date.Windows.csproj` 通过
- `0 warning / 0 error`
