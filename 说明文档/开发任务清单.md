# 开发任务清单

## 已完成

- [x] 阅读项目说明文档、AI 开发指导、质量要求和重构优化要求。
- [x] 修复启动链，使用自定义运行时 `Game` 作为工程入口。
- [x] 搭建第一版运行时原型注入机制。
- [x] 建立程序化地形基础设施目录 `Game58date/Terrain`。
- [x] 实现确定性噪声 `DeterministicNoise`。
- [x] 实现区块坐标、区块数据、方块类型和地形生成参数对象。
- [x] 实现第一版区块生成器 `TerrainChunkGenerator`。
- [x] 实现第一版体素区块网格器 `VoxelChunkMesher`。
- [x] 实现运行时模型上传工厂 `VoxelChunkModelFactory`。
- [x] 实现运行时区块装载脚本 `VoxelTerrainRuntimeScript`。
- [x] 让程序化地形版本成为默认启动流程。
- [x] 验证 `dotnet build Game58date.Windows\\Game58date.Windows.csproj` 通过。
- [x] 将地形生成升级为世界场采样结构，加入 biome 判定与洞穴密度采样。
- [x] 将区块网格器升级为贪心网格实现。
- [x] 将区块运行时脚本拆分为场景引导、世界运行时、调试覆盖层和统计对象。
- [x] 加入区块后台生成队列与主线程集成分离。
- [x] 将简单列填充地形升级为 3D 密度场地形，加入挑檐、悬崖、坡度表层分布。
- [x] 引入基础运行时地形材质，替换球体材质的临时复用方案。
- [x] 修复区块世界坐标放置错误，避免所有区块堆叠在原点。
- [x] 收紧洞穴 carve 规则，避免表层被打穿成瑞士奶酪。
- [x] 修复体素网格绕序错误，恢复外表面与内部面的正确 front-face 判定。
- [x] 编写体素地形网格问题复盘文档。
- [x] 将体素六个面重构为显式几何定义，并增加启动期面绕序自检。
- [x] 将 biome 从硬切换升级为平滑权重场，并加入地表材质语义解析层。
- [x] 为区块生成与卸载增加贯穿请求、构建、丢弃、集成、卸载的运行时日志。
- [x] 引入程序化地形纹理图集。
- [x] 增加水体独立网格层与独立水材质。
- [x] 增加区块碰撞体生成。
- [x] 增加方块覆盖存储与脏区块/邻区块重建基础链路。
- [x] 将区块可视实体与碰撞实体解耦，修复物理回写 transform 导致的区块上浮/错层风险。
- [x] 增加真正可行走的第一人称角色控制链路，并保留观察者模式切换。
- [x] 修复出生点初始化，预热出生区块与碰撞后再生成角色，避免生在地形下方直接坠落。
- [x] 修复模式切换逻辑，观察者与第一人称切换时保持当前位置不变，并增加独立重置位置入口。
- [x] 将第一人称从独立物理角色重构为相机实体上的自定义体素角色控制器，彻底统一观察者/第一人称坐标。
- [x] 完成第一阶段画质与抗锯齿优化，移除无效 MSAA 配置，恢复高质量 FXAA，提升纹理质量与图集采样稳定性。
- [x] 完成第二阶段地形材质链路升级，引入按面纹理解析与高质量程序图集骨架，为后续高级地形材质方案奠定基础。
- [x] 搭建正式世界规律引擎，落地基础世界状态模型、规则推进和征兆触发骨架。
- [x] 搭建正式行为画像与因果记录系统，并接入默认地形运行时与存档。

## 进行中

- [ ] 继续以最高标准优化画质与抗锯齿，向真正的地形高级材质方案演进，包括三向投射、材质分层和更高质量远景观感。
- [ ] 继续提升地形材质质量，向真正的三向投射或更高质量图集方案演进。

## 待完成

- [x] 增加存档格式、世界种子配置和加载恢复流程。
- [ ] 增加远景、LOD、性能统计和对象池。
- [ ] 将旧原型脚本迁移到单独模式，避免与地形启动链长期混用。
- [ ] 在完成关键系统后持续更新此清单与重构日志。
